# #!/bin/bash

# set -e

# VERSION=$1
# FILE="SemanticReleaseTest.csproj"

# if [ -z "$VERSION" ]; then
#   echo "No version provided!"
#   exit 1
# fi

# # Use sed to update the version in the .csproj file
# sed -i '' -e "s|<Version>.*</Version>|<Version>$VERSION</Version>|" $FILE

# echo "Version updated to $VERSION"
